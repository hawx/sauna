<!DOCTYPE html>  
<html lang="en">  
  <head>  
    <meta charset="utf-8" />  
  	<title><%= @title %></title>
  	<link rel="stylesheet" href="/css/screen.css" type="text/css" />
  	<script src="/js/jquery.min.js" type="text/javascript"></script>
  	<script src="/js/hashgrid.js" type="text/javascript"></script>
  	<script src="/js/form.js" type="text/javascript"></script>
  </head>
  
  <body> 
    <div id="wrapper">
      <% if session[:notice] != "" %>
        <div id="flash">
          <span><%= session[:notice] %></span>
        </div>
      <% end %>
      
      <header>
        <h1><a href="/"><%= @sauna.name %></a></h1>
        <div class="meta">
        <% if logged_in? %>
          You're logged in as <a href="<%= current_user.url %>"><%= current_user.username %></a> (<%= current_user.rank %>) 
          <a href="/logout">Logout</a>
        <% else %>
          You're not logged in <a href="/login">Login?</a>
        <% end %>
        </div>
      </header>
      
      <%= yield %>
      
      <footer>Powered by <a href="http://github.com/hawx/sauna">Sauna</a></footer>
    </div>
  </body>
</html>